const Transfer = () => {
  return (
    <div className="rounded-xl border border-ink/5 bg-surface px-3 py-2 text-sm font-semibold text-ink/70">
      Transfer flow has been disabled in this build.
    </div>
  );
};

export default Transfer;
